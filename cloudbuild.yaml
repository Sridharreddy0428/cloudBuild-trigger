# This yaml file will create an image in the existed artifact repository quickstart-docker-repo 
# To run this yaml file run the below cmd
# gcloud builds submit --region=us-west2 --config cloudbuild.yaml 
# steps:
# - name: 'gcr.io/cloud-builders/docker'
#   args: [ 'build', '-t', 'us-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag1', '.' ]
# images:
# - 'us-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag1'

steps:
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/quickstart-docker-repo:$COMMIT_SHA', '.']
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/quickstart-docker-repo:$COMMIT_SHA']
# Deploy container image to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - 'run'
  - 'deploy'
  - 'quickstart-docker-repo'
  - '--image'
  - 'gcr.io/$PROJECT_ID/quickstart-docker-repo:$COMMIT_SHA'
  - '--region'
  - 'us-west2'
 images:
 - 'gcr.io/$PROJECT_ID/quickstart-docker-repo:$COMMIT_SHA'
# # adding deployment configuration to cloud run
# steps:
# - name: 'gcr.io/cloud-builders/gcloud'
#   args:
#   - 'run'
#   - 'deploy'
#   - 'cloudrunservice'
#   - '--image'
#   - 'us-west2-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag1'
#   - '--region'
#   - 'us-west2'
#   - '--platform'
#   - 'managed'
#   - '--allow-unauthenticated'


